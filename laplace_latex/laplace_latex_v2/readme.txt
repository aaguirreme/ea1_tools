
Requirements:
* LaTeX distribution.
* LaTeX packages: flashcards, amsmath, tikz.
* latexmk (optional, for automatic compilation).

To compile the pdf using latexmk, run the following command in a MacOS or Linux
computer.

  latexmk -pdf

To clean the auxiliary files after compilation, run the following command.

  latexmk -c
