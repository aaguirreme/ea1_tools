
Requirements:
* LaTeX distribution.
* LaTeX packages: flashcards, mathtools, tikz.
* latexmk (optional, for automatic compilation).

To compile the pdf using latexmk, run the following command in MacOS or Linux
operating system.

  latexmk -pdf

To clean the auxiliary files after compilation, run the following command.

  latexmk -c
